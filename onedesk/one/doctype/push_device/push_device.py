"""A browser somebody turned push on in, written by one/push.py. Nobody opens
it on the desk: a person sees and removes their own under Settings,
Notifications."""

from frappe.model.document import Document


class PushDevice(Document):
	pass
